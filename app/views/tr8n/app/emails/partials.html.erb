<%= tr8n_with_options_tag(:default_locale => Tr8n::Config.default_admin_locale, :admin => true) do %>

  <%= render :partial => "/tr8n/app/common/header" %>
  <div class="tr8n tr8n_container">

    <div class="section_title">
      <%=render(:partial => "sections") %>
      <%=tra("Email Partials") %>
    </div>

    <div class="section grey">
      <div class="tr8n_float_right" style="padding-right:5px;">
        <%=tr8n_help_icon_tag("Email Partials") %>
      </div>

      <div class="tr8n_text_align_left" style="padding-top:5px;padding-bottom:20px;font-size:12px;">
        <%= tra("Partials are reusable blocks that can be inserted inside email templates, email layouts and even other partials.") %>
        <%= tra("To include a partial inside of a template, use the following tag: [bold: {tag}].", :tag => "{% partial KEYWORD %}") %>
      </div>

      <div class="text-right">
        <%=link_to("Add Email Partial", {:action => :email_partial_wizard}, {"class" => "btn btn-primary", "data-toggle" => "dynamic_wizard"})%>
      </div>
    </div>

    <div class="section">
      <%= tr8n_paginator_tag(@partials, :subject => 'partial') if @partials.any? %>

      <table class="table table-hover">
        <thead>
        <tr>
          <th style="width:100px;"><%=tra("Keyword") %></th>
          <th style="width:150px"><%=tra("Description") %></th>
          <th style="width:100px;"></th>
        </tr>
        </thead>
        <tbody>
        <% @partials.each do |et| %>
          <tr>
            <td>
              <%= et.keyword%>
            </td>
            <td>
              <%= et.description%>
            </td>
            <td style="text-align:right">
              <%=link_to_function(tra("Preview Text"), "previewEmail('#{et.id}', 'text')", :class => "btn") %>
              <%=link_to_function(tra("Preview Html"), "previewEmail('#{et.id}', 'html')", :class => "btn") %>
              <%=link_to(tra("Edit"), {:action => :template, :id => et.id}, :class => "btn") %>
            </td>
          </tr>
        <% end %>
        </tbody>
      </table>

    </div>

  </div>
  <%= render :partial => "/tr8n/app/common/footer" %>

  <%= stylesheet_link_tag('tr8n/bootstrap/bootstrap-switch.css') %>
  <%=javascript_include_tag('tr8n/vendor/bootstrap/bootstrap-switch.js') %>

<% end %>
