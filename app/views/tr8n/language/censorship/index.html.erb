<%= tr8n_with_options_tag(:default_locale => Tr8n::Config.default_admin_locale, :admin => true) do %>

  <%= render :partial => "/tr8n/language/common/header" %>
  <div class="tr8n tr8n_container" <%=tr8n_dir_attribute_tag%>>

    <div class="section_title">
      <%= tra("Prohibited Words") %>
    </div>

    <%= form_for(tr8n_current_language, :as => :language, :url => {}, :html => {:id => 'prohibited_words_form', :method => :post}) do |f| %>
      <div id="prohibited_words_content" class="section grey">
        <%=f.text_area(:curse_words, :style => "width:99%; height:200px; background:white;")%>

        <div style="float:right;">
          <button type="submit" class="translator_btn translator_submit_btn" onClick="Tr8n.submit('basic_info_form'); return false;">
            <span><%=tra("Update")%></span>
          </button>
        </div>

        <div class="tr8n_txtl"  style="color:grey; font-size:10px; padding-top:10px;"><%=tra("Add any prohibited/curse words that should not be allowed in translations for this settings. Separate words with commas.")%></div>

      </div>
    <% end %>

  </div>

  <%= render :partial => "/tr8n/common/footer" %>

<% end %>
