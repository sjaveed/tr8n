<style>
  label {
    font-weight:bold;
    padding-top:15px;
  }
</style>
<%= tr8n_with_options_tag(:default_locale => Tr8n::Config.default_admin_locale, :admin => true) do %>

  <%= render :partial => "/tr8n/app/common/header" %>
  <div class="tr8n tr8n_container">

    <div class="section_title">
      <%=render(:partial => "sections") %>
      <%=tra("Style Guides") %>
    </div>

    <div class="section grey">
      <div class="tr8n_fltr" style="padding-right:5px;">
        <%=tr8n_help_icon_tag("Application Settings") %>
      </div>

      <div class="tr8n_txtl" style="padding-top:5px;padding-bottom:20px;font-size:12px;">
        <%= tra("You can configure what the page elements look like for each state.") %>
      </div>
    </div>

    <div class="section">

      <table class="table table-hover">
        <thead>
        <tr>
          <th style="width:150px;">Class</th>
          <th>Style</th>
          <th></th>
        </tr>
        </thead>
        <tbody>
        <% selected_application.classes.each do |key, style| %>
          <tr>
            <td>
              <strong><%= key %></strong>
            </td>
            <td>
              <div> <span style="<%=style%>">sample</span> </div>
              <pre><code class="language-css"><%= style %></code></pre>
            </td>
            <td style="text-align:right">
              <%=link_to("<i class='icon-wrench'></i>".html_safe, {:action => :stylesheet_modal, :key => key}, {"data-toggle" => "dynamic_modal"})%>
            </td>
          </tr>
        <% end %>
        </tbody>
      </table>

    </div>

  </div>
  <%= render :partial => "/tr8n/app/common/footer" %>
  <%=javascript_include_tag('tr8n/vendor/prism.js') %>
  <%= stylesheet_link_tag('tr8n/vendor/prism.css') %>

<% end %>

